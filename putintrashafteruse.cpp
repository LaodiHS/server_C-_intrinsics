#include <algorithm>
#include <iostream>
using namespace std;

int func(int len, int &arr, int look) {
    int half = len / 2;
    int *s = arr + half;

    while (s < (arr + len) && s >= arr)
    {







  }
}

int main() {
  int arr[50] = {1, 2, 3, 4, 5, 6};

  qsort(arr) func()

          cout
      << arr << endl;
}